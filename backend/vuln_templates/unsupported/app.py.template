from flask import Flask
app = Flask(__name__)

@app.route('/')
def home():
    return """
    <h3>No training environment available yet</h3>
    <p>CVE: {cve_id}</p>
    <p>This vulnerability's exploitation mechanism isn't covered by any
    available training template yet. Ask an admin to add support for it.</p>
    """

if __name__ == '__main__':
    app.run(host='0.0.0.0', port=80)